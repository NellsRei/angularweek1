import { SpinningDirective } from './spinning.directive';

describe('SpinningDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinningDirective();
    expect(directive).toBeTruthy();
  });
});
